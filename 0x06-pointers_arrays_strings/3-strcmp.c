#include "main.h"
#include<string.h>

/**
 * _strcmp -> it compares
 * @s1: 1st param
 * @s2: 2nd param
 * Return: intiger
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
